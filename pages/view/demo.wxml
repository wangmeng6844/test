<view class='wrap'>
<view class='banner'>
<swiper indicator-dots="{{indicatorDots}}" indicator-active-color="{{color}}"
  autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
  <block wx:for="{{imgUrls}}" wx:key="{{index}}">
    <swiper-item>
      <image src="{{item}}" class="slide-image" width="355" height="150"/>
    </swiper-item>
  </block>
</swiper>
</view>
<title/>
<list/>
<list-image/>
</view>

